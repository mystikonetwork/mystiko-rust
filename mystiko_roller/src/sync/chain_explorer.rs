// todo support xscan

pub struct SyncChainExplorer {
    // url: String,
    // key: String,
}

impl SyncChainExplorer {
    pub fn new(url: &str) -> Self {
        let _ = url;
        // let key = load_chain_explorer_api_key().unwrap();
        SyncChainExplorer {}
    }
}
