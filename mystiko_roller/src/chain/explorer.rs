// todo support xscan

pub struct ExplorerStub {
    // url: String,
    // key: String,
}

impl ExplorerStub {
    pub fn new(url: &str) -> Self {
        let _ = url;
        // let key = load_chain_explorer_api_key().unwrap();
        ExplorerStub {}
    }
}
