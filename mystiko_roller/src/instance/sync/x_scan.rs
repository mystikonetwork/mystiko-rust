// todo support xscan

pub struct XScanInstance {
    // url: String,
    // key: String,
}

impl XScanInstance {
    pub fn new(url: &str) -> Self {
        let _ = url;
        // let key = load_x_scan_api_key().unwrap();
        XScanInstance {}
    }
}
